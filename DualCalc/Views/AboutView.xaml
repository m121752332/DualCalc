<Page
    x:Class="DualCalc.Views.AboutView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:svc="using:DualCalc.Services"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <StackPanel Spacing="16" HorizontalAlignment="Center">

            <!-- App Icon -->
            <Image Source="ms-appx:///Assets/Logo.jpg" 
                   Width="200" Height="200" 
                   Stretch="Uniform" 
                   HorizontalAlignment="Center" />

            <!-- App Name -->
            <TextBlock Text="{x:Bind Loc.About_Title, Mode=OneWay}"
                       Style="{ThemeResource TitleTextBlockStyle}"
                       HorizontalAlignment="Center"
                       Visibility="Collapsed" />

            <!-- Version -->
            <TextBlock Text="{x:Bind Loc.About_Version, Mode=OneWay}"
                       Style="{ThemeResource BodyTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       HorizontalAlignment="Center"/>

            <MenuFlyoutSeparator/>

            <!-- Tech Stack -->
            <StackPanel Spacing="8" HorizontalAlignment="Center">
                <!-- Frontend badge -->
                <Border CornerRadius="6" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        Padding="16,10">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <TextBlock Text="🖥" FontSize="16" VerticalAlignment="Center"/>
                        <TextBlock Text="{x:Bind Loc.About_Frontend, Mode=OneWay}"
                                   Style="{ThemeResource BodyTextBlockStyle}"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Backend badge -->
                <Border CornerRadius="6" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        Padding="16,10">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <TextBlock Text="⚙" FontSize="16" VerticalAlignment="Center"/>
                        <TextBlock Text="{x:Bind Loc.About_Backend, Mode=OneWay}"
                                   Style="{ThemeResource BodyTextBlockStyle}"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Copyright -->
            <TextBlock Text="{x:Bind Loc.About_Copyright, Mode=OneWay}"
                       Style="{ThemeResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       HorizontalAlignment="Center"
                       Margin="0,8,0,0"/>
        </StackPanel>
    </Grid>
</Page>
